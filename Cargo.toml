[package]
name = "parking-iot-rust"
version = "0.1.0"
edition = "2021"
description = "High-performance parking IoT management system written in Rust"
license = "MIT"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
tokio-tungstenite = "0.20"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
warp = "0.3"
rumqttc = "0.24"
uuid = { version = "1.0", features = ["v4"] }
tracing = "0.1"
tracing-subscriber = "0.3"
futures-util = "0.3"
tower = "0.4"
tower-http = { version = "0.4", features = ["fs", "cors"] }
clap = { version = "4.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true